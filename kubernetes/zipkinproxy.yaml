---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: honeycomb-opentracing-proxy
  # Putting this in the istio-system namespace makes it work with Istio (for
  # Istio 0.2)
  namespace: istio-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: honeycomb-opentracing-proxy
    spec:
      containers:
      - name: honeycomb-opentracing-proxy
        image: honeycombio/honeycomb-opentracing-proxy
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9411
        command:
          - /go/bin/honeycomb-opentracing-proxy
        args:
          - -d
          - zipkin-test
          - -k
          - "$(HONEYCOMB_WRITEKEY)"
        env:
        - name: HONEYCOMB_WRITEKEY
          valueFrom:
            secretKeyRef:
              key: key
              name: honeycomb-writekey

---
apiVersion: v1
kind: Service
metadata:
  # pretend to be zipkin for istio demo purposes
  name: zipkin
  labels:
    app: zipkin
  namespace: istio-system
spec:
  ports:
  - name: honeycomb-opentracing-proxy
    port: 9411
    targetPort: 9411
  selector:
    app: honeycomb-opentracing-proxy
